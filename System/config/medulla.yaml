medulla:
  state_parameters:
    awake_port: 8080
    max_daily_token_budget: 500000

  circadian_rhythm:
    sleep_trigger_time: "03:00"      # 24h cycle trigger clock
    daydream_duration_minutes: 45
    auto_purge_lymph_nodes: false    # If true, runs 'purge' instead of just 'flush'

  background_daemons:
    file_watcher:
      enabled: true
      polling_throttle_ms: 1000
      targets: ["Personal", "Professional", "Studio"]

    dermis_receptor:
      enabled: true
      secure_port: 8080
      auto_tunnel_on_wake: false    # Automatically runs the SSH expose reflex on start
